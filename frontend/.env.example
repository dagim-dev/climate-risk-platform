# Local development
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Error monitoring (optional — set DSN from sentry.io project settings)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# Stripe (use price IDs from your Stripe dashboard)
NEXT_PUBLIC_STRIPE_PRICE_INDIVIDUAL=
NEXT_PUBLIC_STRIPE_PRICE_PROFESSIONAL=
NEXT_PUBLIC_STRIPE_PRICE_BUSINESS=

# Production (set in Vercel Environment Variables — never commit real values)
# NEXT_PUBLIC_API_URL=https://api.climaterisk.io
